<view wx:if="{{visible}}" style="{{css}}" class="model-mask {{animation?animation:''}}" bindtap='hide' catchtouchmove='none'>
  <view class="model-mask-content {{position}}">
    <slot></slot>
  </view>
</view>